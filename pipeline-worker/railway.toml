[build]
builder = "nixpacks"
buildCommand = "npm install -g tsx && npm install"

[deploy]
startCommand = "tsx src/worker.ts"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
